Note: 

this project use firebase function as a server to get summerized notes

the firebase use appcheck so only the app can send request to the firebase server

in debug please look for the
<b>Enter this debug secret into the allow list in the Firebase Console for your project:</b>
and add the debug key to the firebase console so it can work

have fun :)
